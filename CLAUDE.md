# Project conventions

- Use Jujutsu (`jj`) with the git backend for version control in this repo, not raw `git`.
  Repo is already colocated (`.jj` + `.git`). Use `jj log`, `jj st`, `jj new`, `jj describe`,
  `jj bookmark`, `jj git push` etc. instead of `git add/commit/checkout/branch`.
